clear all;

dimList = [33, 65, 129];
NList = length(dimList);
na = 3; nb = 3;
omega = 0.8;
for i = 1:NList
    dim = dimList(i);
    u = zeros(dim, dim);
    f = zeros(dim, dim);
    f(2:end-1, 2:end-1) = 1;
    space = 1/dim;
    f_loc = [0; 0; space; space];
    u_loc = [0; 0; space; space];

    save 'MyInput.mat' '-v4' ;
    s1 = 'echo';
    command = strcat(s1, 
    system(command);
%    !./mgv $NList $na $nb $omega $i
    load('OutputAll.mat');
    plot(r);
end